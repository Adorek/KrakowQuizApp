<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="question_style" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Style for questions. -->
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@android:color/black</item>
        <item name="android:textSize">@dimen/questions_style</item>
        <item name="android:paddingBottom">@dimen/questions_padding_bottom</item>
        <item name="android:paddingTop">@dimen/questions_padding_top</item>
    </style>
    <style name="separation_line" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Style of separation line. -->
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">1dp</item>
        <item name="android:background">@android:color/darker_gray</item>
        <item name="android:layout_marginTop">@dimen/separation_line_margin_top</item>
    </style>

</resources>
